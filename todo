
---

# **一、项目核心目标**
| 需求                | 解决方案                                                                 |
|---------------------|--------------------------------------------------------------------------|
| **跨平台展示**       | 主要是UI大小的适配                          |
| **字符效果**         | 颜色渐变、发光、加粗、跳动（逐字符独立控制）                             |
| **运动效果**         | 滚动（水平/垂直）、跳动（整体/局部）、缩放、淡入淡出                     |
| **低延迟**           | 优化移动端性能（避免每帧遍历所有字符）                                   |
| **易用性**           | 通过 `Inspector` 直接设置效果（无需写代码）                              |

---

# **二、架构设计（Godot 4.6 项目结构）**
## **文件目录结构**
```plaintext
TextFlow/
├── main.tscn          # 主场景
├── src/               # 核心
│   ├── text_flow_manager.gd/tscn # 文本管理核心（控制所有效果）
│   ├── text_character.gd/tscn   # 逐字符节点（继承自Label）
│   └── effect.gd/tscn     # 效果工具类（复用逻辑）
│   └── editor_ui.gd/tscn      # 配置编辑器, 用户输入文字后可以自己分段，每一段可以有自己的效果
├── fonts/                 # 字体资源（含中文字体，如思源黑体），里面需要获取系统字符
│   └── SourceHanSans.ttf
├── effects/                # 效果预设
│   ├── glow.shader          # 发光效果预设
│   └── scroll.shader        # 滚动效果预设
└── export/                # 导出配置（Android/iOS等）
```

---

# **三、关键组件设计（核心逻辑）**

# 1. TextFlowManager（核心管理器）
**作用**：控制文本显示动画、效果调度、跨平台适配。

- **工作流程**：
  1. `editor_ui` 拆分文本为字段 → 创建 `TextCharacter` 节点。
  2. 通过 `AnimationPlayer` 统一管理动画（避免每帧遍历字符）。
  3. 自动适配屏幕尺寸（移动端自动缩放）。

## 2. TextCharacter（逐字符节点）
**作用**：每个字符独立控制颜色、发光、跳动等效果，也就是单个文本字符的表现形式。
text_character 是由 editor_ui 里面分配等字段给出来的
- **关键属性**（在 Inspector 中可调）：
  ```gdscript
  var text: String = "欢迎应援！"  # 文本内容
  var font: Font = load("fonts/SourceHanSans.ttf") # 字体
  var font_size: int = 48          # 字体大小（自动缩放）
  var color: Color = Color(1, 0.2, 0.2) # 基础颜色
  var outline_color: Color = Color(1, 1, 0) # 发光颜色
  var outline_size: int = 3        # 发光宽度
  var scroll_speed: float = 50.0    # 滚动速度（像素/秒）
  var jump_intensity: float = 0.1   # 跳动强度（0~1）
  var enable_jump: bool = true      # 启用跳动效果
  ```


## **3. 效果实现方案**
| 效果类型       | 实现方式                                                                 | Godot 4.6 关键技术               |
|----------------|--------------------------------------------------------------------------|----------------------------------|
| **颜色**       | `color` 属性 + `Tween` 渐变（如从红→蓝）                                | `Tween` + `Color.interpolate()` |
| **发光**       | `outline_color` + `outline_size`（或者使用 Shader）                      | `Label` 原生属性                |
| **加粗**       | 预加载加粗字体（`Font` 资源），通过 `font` 属性切换                     | `Font` 资源的 `weight` 属性      |
| **滚动**       | `TextFlowManager` 移动整个文本容器（水平/垂直滚动）                      | `AnimationPlayer` 或 `position` 动画 |
| **跳动**       | 逐字符应用 `TextCharacter.apply_jump()`（通过 `AnimationPlayer` 批量控制）| `AnimationPlayer` + `Tween`      |

---



# **四、跨平台适配方案**
## 输入
使用 `Input` 捕获键盘（如按 `Space` 暂停滚动， ⬅️ 向左1s等）
不同平台的输入事件（如触摸、鼠标）通过 `Input` 统一处理，确保在 PC 和移动端都能流畅操作。

## 分辨率
应该适配不同屏幕大小，包含桌面的读稿模式

---

### **扩展接口保留**
- 3D 效果：用 `MeshInstance3D` 实现文字悬浮效果等，保留接口
- 音频同步：未来可以添加音频分析接口，实现文字随音乐节奏跳动等效果